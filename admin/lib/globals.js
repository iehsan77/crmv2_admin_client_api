import { PUBLIC_PATHS as PUBLIC_PATHS } from "@/constants/paths";
import { ADMIN_PATHS as ADMIN_PATHS } from "@/constants/paths";
globalThis.ADMIN_PATHS = ADMIN_PATHS;
globalThis.PUBLIC_PATHS = PUBLIC_PATHS;